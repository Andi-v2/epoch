export default interface Character {
    id: string
    name: string
    age: number | string
    owner: string
    faction: string
    role: string
    image: string
    bio: string
    characteristics: string[]
    species: string
}