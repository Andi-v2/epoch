export default interface Character {
    id: string
    name: string
    age: number
    owner: string
    faction: string
    role: string
    image: string
    bio: string
    skills: string[]
    background: string
}